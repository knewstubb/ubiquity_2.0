import { ModalFilterBuilder } from './modal/modal-filter-builder'
import type { ModalFilterBuilderProps } from './types'

export function FilterBuilder(props: ModalFilterBuilderProps) {
  return <ModalFilterBuilder {...props} />
}
